spring:
  datasource:
    url: jdbc:h2:mem:hotel_booking_test;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  jwt:
    secret: test-secret-key-for-unit-tests-only-minimum-256-bits-long!!
    expiration-ms: 3600000
